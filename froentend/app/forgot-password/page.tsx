import ComingSoonPage from '@/components/ComingSoonPage';

export default function ForgotPasswordPage() {
  return (
    <ComingSoonPage
      eyebrow="Account Recovery"
      title="Forgot Password"
      description="Password recovery is planned for Echoes, but it is not available yet. This placeholder now leads to a real page instead of a dead link."
    />
  );
}
